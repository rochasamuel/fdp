# Colyseus reads this file at boot. Keep it here even if empty, otherwise the
# loader walks up the tree and picks up unrelated environment files.
PORT=8080
# Origens que podem falar com o matchmaking, separadas por vírgula. Sem ela,
# qualquer origem passa.
# CORS_ORIGIN=https://fdp.example.com,https://fdp-staging.example.com
