{
  "$schema": "https://schema.railpack.com",
  "provider": "node",
  "steps": {
    "build": {
      "inputs": [{ "step": "install" }],
      "commands": ["pnpm --filter server... build"]
    }
  },
  "deploy": {
    "startCommand": "cd apps/server && node build/index.js"
  }
}
